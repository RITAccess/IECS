#include "MotorUtilities.nxc"
#define POWER 100
#define DEGREES 21000000
task main()
{
	RotateMotors(MOTOR_A, POWER,DEGREES, 0);
	RotateMotors(MOTOR_AC, POWER, DEGREES, 0);
	RotateMotors(MOTOR_C, POWER, DEGREES, 0);
	RotateMotors(MOTOR_C, POWER, DEGREES, 0);
	RotateMotors(MOTOR_A, POWER, DEGREES, 0);
}
